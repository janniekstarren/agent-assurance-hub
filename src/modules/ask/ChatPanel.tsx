/** Placeholder — replaced by the full Ask assistant in Module 8. */

export function ChatPanel(_props: { seed?: string | null; embedded?: boolean }) {
  return <div style={{ padding: 16 }}>Assistant coming online…</div>;
}
