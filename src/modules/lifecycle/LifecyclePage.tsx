import { PageContainer, SectionTitle } from '../../components/primitives';

export function LifecyclePage() {
  return (
    <PageContainer>
      <SectionTitle title="Lifecycle" caption="Multi-env ALM & approvals — building…" />
    </PageContainer>
  );
}
