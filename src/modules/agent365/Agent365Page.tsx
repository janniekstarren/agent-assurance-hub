import { PageContainer, SectionTitle } from '../../components/primitives';

export function Agent365Page() {
  return (
    <PageContainer>
      <SectionTitle title="Agent 365" caption="Registry & security context — building…" />
    </PageContainer>
  );
}
