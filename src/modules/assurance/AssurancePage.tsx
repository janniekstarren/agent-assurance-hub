import { PageContainer, SectionTitle } from '../../components/primitives';

export function AssurancePage() {
  return (
    <PageContainer>
      <SectionTitle title="Assurance" caption="Accuracy, drift, confidence — building…" />
    </PageContainer>
  );
}
