import { PageContainer, SectionTitle } from '../../components/primitives';

export function OverviewPage() {
  return (
    <PageContainer>
      <SectionTitle title="Overview" caption="Estate health — building…" />
    </PageContainer>
  );
}
