import { PageContainer, SectionTitle } from '../../components/primitives';

export function CostPage() {
  return (
    <PageContainer>
      <SectionTitle title="Cost & consumption" caption="Credits, licensing, budgets — building…" />
    </PageContainer>
  );
}
