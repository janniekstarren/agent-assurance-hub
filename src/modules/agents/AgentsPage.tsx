import { PageContainer, SectionTitle } from '../../components/primitives';

export function AgentsPage() {
  return (
    <PageContainer>
      <SectionTitle title="Agents" caption="Inventory — building…" />
    </PageContainer>
  );
}
